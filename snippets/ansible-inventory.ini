🐧 Variant 1: Simple inventory.ini
ini
[webservers]
192.168.1.10
192.168.1.11
👉 Explanation:

[webservers] defines a group of hosts.

Each line is an IP or hostname.

Useful for quick testing or small deployments.

=================================================================================================================

🐧 Variant 2: Multi‑Group Inventory
ini
[webservers]
web1.example.com
web2.example.com

[dbservers]
db1.example.com
db2.example.com

[loadbalancers]
lb1.example.com
👉 Explanation:

Multiple groups (webservers, dbservers, loadbalancers).

Lets you target specific tiers in playbooks (hosts: dbservers).

Scales better for multi‑tier applications.

=================================================================================================================

🐧 Variant 3: Production‑Grade Inventory with Variables
ini
[webservers]
web1 ansible_host=192.168.1.10 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa app_port=8080
web2 ansible_host=192.168.1.11 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa app_port=8080

[dbservers]
db1 ansible_host=192.168.1.20 ansible_user=postgres ansible_ssh_private_key_file=~/.ssh/id_rsa db_port=5432
db2 ansible_host=192.168.1.21 ansible_user=postgres ansible_ssh_private_key_file=~/.ssh/id_rsa db_port=5432

[all:vars]
ansible_python_interpreter=/usr/bin/python3

👉 Explanation:

Each host can have host‑specific variables (app_port, db_port).

ansible_host → actual IP address.

ansible_user → SSH user.

ansible_ssh_private_key_file → path to SSH key.

[all:vars] → global variables applied to all hosts (e.g., Python interpreter).

This structure is production‑ready: secure, flexible, and environment‑aware.

✅ Summary
Simple → quick dev/test setup.

Multi‑group → organize hosts by role (web, db, load balancer).

Production‑grade → host variables, SSH keys, global vars, ready for secure automation.

EXAMPLE:
[nginx]
13.220.148.203 ansible_user=ubuntu ansible_ssh_private_key_file=~/viskey.pem

[appservers]
10.0.2.250 ansible_user=ubuntu ansible_ssh_private_key_file=~/viskey.pem
10.0.2.206 ansible_user=ubuntu ansible_ssh_private_key_file=~/viskey.pem

[appservers:vars]
ansible_ssh_common_args='-o ProxyCommand="ssh -i ~/viskey.pem -W %h:%p ubuntu@13.220.148.203"'
